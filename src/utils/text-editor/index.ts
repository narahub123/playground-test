import { setCursorPosition } from "./set";

import {
  moveRight,
  moveLeft,
  moveUp,
  moveDown,
  moveStart,
  moveEnd,
} from "./move";

import {
  getXCoord,
  getTargetAndRemainedLength,
  getIndexInTarget,
  getTargetAndIndex,
} from "./get";

export {
  setCursorPosition,
  moveRight,
  moveLeft,
  moveUp,
  moveDown,
  moveStart,
  moveEnd,
  getXCoord,
  getTargetAndRemainedLength,
  getIndexInTarget,
  getTargetAndIndex,
};
