import { setCursorPosition } from "./set";

import { moveRight, moveLeft, moveUp, moveDown, moveStart } from "./move";

import {
  getXCoord,
  getTargetAndRemainedLength,
  getIndexInTarget,
  getTargetAndIndex,
} from "./get";

export {
  setCursorPosition,
  moveRight,
  moveLeft,
  moveUp,
  moveDown,
  moveStart,
  getXCoord,
  getTargetAndRemainedLength,
  getIndexInTarget,
  getTargetAndIndex,
};
