import { setCursorPosition } from "./set";

import {
  moveRight,
  moveLeft,
  moveUp,
  moveDown,
  moveStart,
  moveEnd,
  movePageUp,
  movePageDown,
} from "./move";

import {
  getXCoord,
  getTargetAndRemainedLength,
  getIndexInTarget,
  getTargetAndIndex,
  getCurElement,
} from "./get";

export {
  setCursorPosition,
  moveRight,
  moveLeft,
  moveUp,
  moveDown,
  moveStart,
  moveEnd,
  movePageUp,
  movePageDown,
  getXCoord,
  getTargetAndRemainedLength,
  getIndexInTarget,
  getTargetAndIndex,
  getCurElement,
};
