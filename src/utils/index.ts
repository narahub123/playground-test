import {
  createNewLine,
  moveup,
  movedown,
  moveLeft,
  moveRight,
  createNormalSpan,
  setCursorPosition,
  hasLink,
  getCurElement,
  deleteByBackspace,
  moveStart,
  moveEnd,
  movePageUp,
  movePageDown,
  selectToEnd,
  selectToStart,
  selectWithPgUp,
  selectWithPgDn,
  selectWithArrowRight,
  selectWithArrowLeft,
  selectWithArrowUp,
  selectWithArrowDown,
  initializeSelection,
} from "./text-editor";

export {
  createNewLine,
  moveup,
  movedown,
  moveLeft,
  moveRight,
  createNormalSpan,
  setCursorPosition,
  hasLink,
  getCurElement,
  deleteByBackspace,
  moveStart,
  moveEnd,
  movePageUp,
  movePageDown,
  selectToEnd,
  selectToStart,
  selectWithPgUp,
  selectWithPgDn,
  selectWithArrowRight,
  selectWithArrowLeft,
  selectWithArrowUp,
  selectWithArrowDown,
  initializeSelection,
};
